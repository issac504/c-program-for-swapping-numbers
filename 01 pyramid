#include <stdio.h>  
#include <conio.h>  
void main()  
{  
    int i, j, rows; 
    rows=6;
    for (i = 1; i <= rows; ++i)  
    {  
        for (j = 1; j <= i; ++j) 
        {  
            printf ("01"); 
        }  
        printf ("\n");   
    }  
    getch();      
}  
